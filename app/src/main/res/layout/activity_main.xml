<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/tv_selected_letter"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_centerInParent="true"
        android:background="@drawable/corner_bg_black_stroke"
        android:gravity="center"
        android:textColor="#ffffff"
        android:textSize="20sp"
        android:visibility="gone" />

    <com.zhj.library.view.LetterSelecterBar
        android:id="@+id/slide_bar"
        android:layout_width="25dp"
        android:layout_height="match_parent"
        app:textSize="14sp"
        app:corner="10dp"
        android:visibility="gone"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/corner_bg"
        app:selectedColor="@color/selectedColor" />

    <com.zhj.library.view.circleselecterbar.CircleSelecterBar
        android:layout_alignParentRight="true"
        android:id="@+id/circle_selector"
        android:layout_marginBottom="10dp"
        app:circle_size="10dp"
        app:big_circle_size="50dp"
        app:circle_color="#ff2222"
        app:circle_padding="10dp"
        android:layout_marginRight="10dp"
        android:layout_marginTop="10dp"
        android:layout_width="wrap_content"
        android:layout_height="match_parent" />
</RelativeLayout>
